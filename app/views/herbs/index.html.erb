<p id="notice"><%= notice %></p>

<h1>Listing Herbs</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @herbs.each do |herb| %>
      <tr>
        <td><%= link_to 'Show', herb %></td>
        <td><%= link_to 'Edit', edit_herb_path(herb) %></td>
        <td><%= link_to 'Destroy', herb, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Herb', new_herb_path %>
